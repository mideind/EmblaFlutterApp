TODO:
    * Implement animations in session button
    * Implement JS execution w. payload from query server (easy via WebViews)
    * Confirmation alert on completion of clear query history calls
    * Make interface 100% nice, styling
    * Local audio playback for UI interaction, errors
    * Update documentation wrt. OS version requirements, building, etc.
    * Add tests + Travis CI integration
    * Send location with query
    * Unique timestamp-based session identifiers to control callbacks
    * Pocketsphinx voice activation
    * Key obfuscation in binary (https://flutter.dev/docs/deployment/obfuscate)
    * Set timeout of HTTP requests to query server, handle timeout
    * Local documentation fallback for documentation web views
